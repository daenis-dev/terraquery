from city_queries_v2 import CITY_QUERIES_V2
from road_queries_v2 import ROAD_QUERIES_V2

print('Number of city queries: ' + str(len(CITY_QUERIES_V2)), flush=True)
print('Number of road queries: ' + str(len(ROAD_QUERIES_V2)), flush=True)
print('Total: ' + str(len(CITY_QUERIES_V2) + len(ROAD_QUERIES_V2)), flush=True)